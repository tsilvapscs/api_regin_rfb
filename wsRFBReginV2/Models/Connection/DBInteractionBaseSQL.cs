////////////////////////////////////////////////////////////////////////////////
// Description: Base class for Database Interaction.                       
// Generated by DALGen32 v1.0.1041.23898 on: Thursday, November 14, 2002, 10:06:04 AM
// Because this class implements IDisposable, derived classes shouldn't do so.
///////////////////////////////////////////////////////////////////////////////
using System;
using System.Configuration;
using System.Data;
using MySql.Data.MySqlClient;
using System.Diagnostics;
using System.Data.SqlClient;

namespace psc.Receita.ConnectionBase
{
    /// <summary>
    /// Purpose: Abstract base class for Database Interaction classes.
    /// </summary>
    public abstract class DBInteractionBaseSQL : IDisposable//, ICommonDBAccess
    {
        #region Class Member Declarations
        protected SqlConnection _mainConnectionSQL;
        protected decimal _errorCode;
        protected bool _mainConnectionIsCreatedLocal;
        protected ConnectionProviderSQL _mainConnectionProviderSQL;
        private bool _isDisposed;
        #endregion


        /// <summary>
        /// Purpose: Class constructor.
        /// </summary>
        public DBInteractionBaseSQL()
        {
            // Initialize the class' members.
            InitClass();
        }


        /// <summary>
        /// Purpose: Initializes class members.
        /// </summary>
        private void InitClass()
        {
            // create all the objects and initialize other members.

            _mainConnectionSQL = new SqlConnection();
            _mainConnectionIsCreatedLocal = true;
            _mainConnectionProviderSQL = null;

            //AppSettingsReader _configReader = new AppSettingsReader();

            // Set connection string of the sqlconnection object
            _mainConnectionSQL.ConnectionString = psc.Framework.General.ConnectionString();
            //_configReader.GetValue("Main.ConnectionString", typeof(string)).ToString();
            _errorCode = (int)DataFactoryError.AllOk;
            _isDisposed = false;
        }


        /// <summary>
        /// Purpose: Implements the IDispose' method Dispose.
        /// </summary>
        public void Dispose()
        {
            Dispose(true);
            GC.SuppressFinalize(this);
        }


        /// <summary>
        /// Purpose: Implements the Dispose functionality.
        /// </summary>
        protected virtual void Dispose(bool isDisposing)
        {
            // Check to see if Dispose has already been called.
            if (!_isDisposed)
            {
                if (isDisposing)
                {
                    // Dispose managed resources.
                    if (_mainConnectionIsCreatedLocal)
                    {
                        // Object is created in this class, so destroy it here.
                        _mainConnectionSQL.Close();
                        _mainConnectionSQL.Dispose();
                        _mainConnectionIsCreatedLocal = false;
                    }
                    _mainConnectionProviderSQL = null;
                    _mainConnectionSQL = null;
                }
            }
            _isDisposed = true;
        }




        #region Class Property Declarations
        public ConnectionProviderSQL MainConnectionProvider
        {
            get
            {
                if (_mainConnectionProviderSQL == null)
                {
                    throw new NullReferenceException("MainConnectionProvider is null");
                }

                return _mainConnectionProviderSQL;
            }
            set
            {
                if (value == null)
                {
                    // Invalid value
                    throw new ArgumentNullException("MainConnectionProvider", "Null passed as value to this property which is not allowed.");
                }

                // A connection provider object is passed to this class.
                // Retrieve the SqlConnection object, if present and create a
                // reference to it. If there is already a MainConnection object
                // referenced by the membervar, destroy that one or simply 
                // remove the reference, based on the flag.
                if (_mainConnectionSQL != null)
                {
                    // First get rid of current connection object. Caller is responsible
                    if (_mainConnectionIsCreatedLocal)
                    {
                        // Is local created object, close it and dispose it.
                        _mainConnectionSQL.Close();
                        _mainConnectionSQL.Dispose();
                    }
                    // Remove reference.
                    _mainConnectionSQL = null;
                }
                _mainConnectionProviderSQL = (ConnectionProviderSQL)value;
                _mainConnectionSQL = _mainConnectionProviderSQL.DBConnection;
                _mainConnectionIsCreatedLocal = false;
            }
        }


        public decimal ErrorCode
        {
            get
            {
                return _errorCode;
            }
        }
        #endregion
    }
}
